# Same certs repo as Cubixa. CI uses MATCH_GIT_BASIC_AUTHORIZATION
# (base64 of "github_username:personal_access_token").
git_url("https://github.com/muyifeiyangcc/ios-certificates.git")

storage_mode("git")

type("appstore")
